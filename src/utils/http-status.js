/**
 * 200
 * @param code
 * @returns {boolean}
 */
export function is200(code) {
  return parseInt(code / 1000000) === 200
}

/**
 * 403
 * @param code
 * @returns {boolean}
 */
export function is401(code) {
  return parseInt(code / 1000000) === 401
}

/**
 * 403
 * @param code
 * @returns {boolean}
 */
export function is403(code) {
  return parseInt(code / 1000000) === 403
}

/**
 * 403
 * @param code
 * @returns {boolean}
 */
export function is404(code) {
  return parseInt(code / 1000000) === 404
}

/**
 * 403
 * @param code
 * @returns {boolean}
 */
export function is407(code) {
  return parseInt(code / 1000000) === 407
}

/**
 * 403
 * @param code
 * @returns {boolean}
 */
export function is500(code) {
  return parseInt(code / 1000000) === 500
}

